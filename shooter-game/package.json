{
  "name": "dead-zone-shooter",
  "version": "1.0.0",
  "description": "3D Survival Zombie Shooter — iOS & Android via Capacitor",
  "scripts": {
    "build:android": "npx cap add android && npx cap sync android && npx cap open android",
    "build:ios": "npx cap add ios && npx cap sync ios && npx cap open ios",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
